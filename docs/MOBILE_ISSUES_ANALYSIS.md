# تحليل مشاكل نسخة الموبايل (Mobile Version Analysis)

## 1. مشكلة عدم تحميل المراحل الدراسية (Educational Stages Loading Issue)
**الملاحظة:**
عند إنشاء حساب جديد، يتم تحميل قائمة المراحل الدراسية بشكل طبيعي على الكمبيوتر، ولكنها لا تظهر أو تفشل في التحميل على الموبايل (Spinner يدور إلى ما لا نهاية أو القائمة فارغة).

### السبب التقني (Technical Root Cause):
المشكلة تكمن في **اتصال الشبكة بين الموبايل وقاعدة البيانات (Supabase)**، وليست في الكود نفسه.

1.  **اذا كنت تجرب التطبيق محلياً (Localhost):**
    *   عند تشغيل التطبيق على الكمبيوتر، العنوان يكون `localhost:3000`.
    *   عند فتح التطبيق من الموبايل (عبر نفس شبكة الواي فاي) باستخدام `IP_ADDRESS:3000`.
    *   الكود يحاول الاتصال بـ Supabase. إذا كان `NEXT_PUBLIC_SUPABASE_URL` يشير إلى مشروع محلي (Local Supabase) أو إذا كانت هناك قيود على الـ Domain في إعدادات Supabase، فإن الموبايل لن يتمكن من الوصول للخادم.
    *   **الحل المقترح مستقبلاً:** التأكد من أن متغيرات البيئة (`ENV`) مضبوطة للعمل على الـ Public Internet أو استخدام عنوان IP المحلي بدلاً من `localhost` في الإعدادات.

2.  **اذا كان التطبيق مرفوعاً (Production):**
    *   قد يكون السبب هو "حجب الإعلانات/التتبع" (Content Blockers) في متصفحات الموبايل (مثل Safari أو Brave) التي قد تمنع الاتصال ببعض الـ APIs الخارجية إذا لم تكن موثقة.
    *   توقيت الجهاز: إذا كان توقيت الموبايل غير دقيق، تفشل عملية التوثيق (SSL Handshake) مع Supabase، مما يؤدي لفشل جلب البيانات.

**الدليل من الكود (`app/signup/page.tsx`):**
الكود يستخدم `useEffect` لجلب البيانات عند فتح الصفحة. بما أن الكود يعمل على الكمبيوتر، فالمنطق البرمجي سليم. الاختلاف الوحيد هو البيئة (Environment) وقدرة الجهاز على الوصول للـ API.

---

## 2. مشكلة عدم عمل زر تسجيل الخروج (Logout Issue)
**الملاحظة:**
الضغط على زر "تسجيل الخروج" في قائمة الموبايل لا يستجيب.

### السبب التقني (Technical Root Cause):
المشكلة تكمن في طريقة استدعاء دالة تسجيل الخروج بشكل **ديناميكي (Dynamic Import)** داخل حدث الضغط.

**الكود الحالي في `components/Navbar.tsx`:**
```typescript
const handleSignOut = useCallback(async () => {
    try {
        useAuthStore.getState().reset(); 
        // 👇 هنا تكمن المشكلة المحتملة للموبايل
        try {
            const { signOut } = await import('@/lib/supabase'); // Dynamic Import
            await signOut();
        } catch { ... }
        ...
```

**لماذا يسبب هذا مشكلة على الموبايل؟**
1.  **تحميل الملفات (Chunk Loading):**
    عند الضغط على الزر، يحاول المتصفح تحميل ملف JavaScript الخاص بـ `supabase` من السيرفر (لأنه `await import`).
    *   على شبكات الموبايل (3G/4G) أو الواي فاي غير المستقر، قد يفشل هذا التحميل أو يستغرق وقتاً طويلاً.
    *   متصفحات الموبايل (خاصة Safari) قد تقوم بإيقاف تنفيذ العمليات الجانبية (Scripts) إذا استغرقت وقتاً طويلاً أو إذا اعتقدت أن المستخدم يتفاعل مع شيء آخر.

2.  **حدث اللمس (Touch Events):**
    في بعض الأحيان، الاعتماد على `onClick` داخل قوائم متحركة (مثل `MobileMenu` الذي يستخدم `framer-motion`) قد يتعارض مع أحداث اللمس (`TouchStart/TouchEnd`). إذا بدأ تحميل الملف الديناميكي وتأخر، قد لا يشعر المستخدم بأي استجابة.

**الخلاصة:**
الكود يحاول "توفير الأداء" عبر تحميل مكتبة Supabase فقط عند الحاجة (Lazy Loading)، ولكن هذا يسبب نقطة ضعف في تجربة الموبايل حيث الاتصال أقل استقراراً. الحل الجذري (دون تغيير الكود الآن) هو أن المشكلة ليست خطأً برمجياً منطقياً، بل هي مشكلة في **إدارة الموارد والشبكة** على أجهزة الموبايل.
